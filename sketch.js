var backgroundImg;

function preload(){
backgroundImg = loadImage("J2CRFv.png")
}

function setup(){
    createCanvas(windowWidth, windowHeight);
}

function draw(){
background(backgroundImg);

}